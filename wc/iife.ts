// Browser bundle entry: defines <nextup-roadmap> on load. Built minified to
// dist/wc/nextup-roadmap.iife.js and copied into every `build --site` output.
import { defineNextupRoadmap } from "./index";

defineNextupRoadmap();
